import MdxLayout from 'src/components/MdxLayout';

export default (props) => <MdxLayout {...props} />;

# Pricing

**Last updated July 09, 2021**

Fast Feedback is still in alpha. It's free to sign up and leave feedback
and $10/month for unlimited sites and feedback.
